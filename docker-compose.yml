version: '3.4'

services:
  stone.api:
    image: ${DOCKER_REGISTRY-}stoneapi
    build:
      context: .
      dockerfile: Stone.API/Dockerfile
  redis:
    image: redis:alpine
    ports:
      - 8081:8081